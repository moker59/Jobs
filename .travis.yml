dist: trusty
language: java
notifications:
  email: false
cache:
  directories:
  - "$HOME/.cache"
script:
  - mvn clean install
deploy:
  provider: releases
  skip_cleanup: true
  api_key: $GH_TOKEN
  file_glob: true
  file:
    - "target/*.jar"
  on:
    tags: true
